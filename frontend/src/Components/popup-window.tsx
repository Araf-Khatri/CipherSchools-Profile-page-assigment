import { FC } from "react";
import PopupAuth from "./popup-auth";

interface PopupProps {
  windowType: String;
}

const PopupWindow: FC<PopupProps> = ({ windowType }: PopupProps) => {
  return <div></div>;
};

export default PopupWindow;
